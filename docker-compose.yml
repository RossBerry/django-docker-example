version: '3'

services:
  db:
    image: postgres
  website:
    build: ./website
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./website:/website
    ports:
      - "8000:8000"
    depends_on:
      - db